# Backend runtime data / secrets
backend/.env
backend/app/data/
backend/data/

# Python
__pycache__/
*.pyc
.venv/
.venv312/
*.egg-info/
.pytest_cache/
.pytest_tmp/
.ruff_cache/
.coverage
htmlcov/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

#
data/*.zip
draft/

# .claude/skills/* are local junctions/symlinks into .agents/skills/ (the
# git-tracked source) so Claude Code can read skills from its own directory
# without duplicating content -- see .claude/settings.json's `ln -s` permission entry
.claude/skills/